-- Left side
require("items.apple")
require("items.aerospace")   -- workspace ID + window icons (replaces front_app)

-- Right side (added right-to-left, so first = rightmost)
require("items.calendar")    -- date/time only
require("items.battery")     -- with icon
require("items.volume")      -- with icon
require("items.wifi")        -- NET label + graph
require("items.system_stats") -- CPU/GPU/MEM widgets + unified popup
