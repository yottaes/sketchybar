-- Left side
require("items.apple")
require("items.aerospace")
require("items.front_app")

-- Right side (added right-to-left, so first = rightmost)
require("items.calendar")
require("items.battery")
require("items.volume")
require("items.wifi")
require("items.system_stats")
-- require("items.menus")  -- disabled: needs Accessibility permission (re-enable after granting)
